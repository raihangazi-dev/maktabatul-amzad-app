import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Admin Publishers"
      description="Admin publisher management route."
    />
  );
}