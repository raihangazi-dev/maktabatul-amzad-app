import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Admin Reports"
      description="Admin reports route."
    />
  );
}