import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Admin Orders"
      description="Admin order management route."
    />
  );
}