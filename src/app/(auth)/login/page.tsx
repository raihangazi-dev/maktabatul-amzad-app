import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Login"
      description="Customer and admin sign-in route."
    />
  );
}