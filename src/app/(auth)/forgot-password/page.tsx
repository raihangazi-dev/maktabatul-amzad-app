import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Forgot Password"
      description="Password recovery request route."
    />
  );
}