import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Account Settings"
      description="Customer account settings route."
    />
  );
}