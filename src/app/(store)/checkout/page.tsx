import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Checkout"
      description="Checkout entry route."
    />
  );
}