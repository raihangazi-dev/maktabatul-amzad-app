import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Checkout Shipping"
      description="Shipping information step route."
    />
  );
}