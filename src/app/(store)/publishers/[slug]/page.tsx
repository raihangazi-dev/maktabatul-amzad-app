import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      title="Publisher Details"
      description="Books filtered by publisher route."
    />
  );
}