import { apiPlaceholder } from "@/lib/api-response";

export async function GET() {
  return apiPlaceholder("Coupons collection", ["GET", "POST"]);
}

export async function POST() {
  return apiPlaceholder("Coupons collection", ["GET", "POST"]);
}

